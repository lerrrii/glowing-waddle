{
  "services": [
    {
      "name": "gluetun",
      "image": "qmcgaw/gluetun:latest",
      "capAdd": ["NET_ADMIN"],
      "devices": ["/dev/net/tun"],
      "ports": [
        {
          "hostPort": "${APP_PORT}",
          "containerPort": 9117
        }
      ],
      "environment": {
        "VPN_SERVICE_PROVIDER": "${JACKETT_VPN_SERVICE_PROVIDER}",
        "VPN_TYPE": "${JACKETT_VPN_TYPE}",
        "WIREGUARD_PRIVATE_KEY": "${JACKETT_WIREGUARD_PRIVATE_KEY}",
        "SERVER_COUNTRIES": "${JACKETT_SERVER_COUNTRIES-Netherlands,United States}",
        "FREE_ONLY": "${JACKETT_FREE_ONLY-on}",
        "FIREWALL": "${JACKETT_FIREWALL-on}",
        "FIREWALL_VPN_INPUT_PORTS": "9117",
        "FIREWALL_OUTBOUND_SUBNETS": "",
        "DNS": "${JACKETT_DNS-on}",
        "DOT": "on",
        "PUBLICIP": "true",
        "HTTPPROXY": "on",
        "HTTPPROXY_PORT": "8888",
        "HTTPPROXY_STEALTH": "on",
        "CONTROL_SERVER_PORT": "8000",
        "SHADOWSOCKS": "off"
      },
      "volumes": [
        {
          "hostPath": "${APP_DATA_DIR}/data/gluetun",
          "containerPath": "/gluetun"
        }
      ],
      "healthcheck": {
        "test": ["CMD", "curl", "-f", "http://localhost:8000/v1/publicip/ip"],
        "interval": "30s",
        "timeout": "10s",
        "retries": 3
      },
      "logging": {
        "driver": "json-file",
        "options": {
          "max-size": "10m"
        }
      }
    },
    {
      "name": "jackett",
      "image": "linuxserver/jackett:latest",
      "isMain": true,
      "internalPort": 9117,
      "networkMode": "service:gluetun",
      "environment": {
        "PUID": "${JACKETT_PUID-1000}",
        "PGID": "${JACKETT_PGID-1000}",
        "TZ": "${TZ-Europe/London}"
      },
      "volumes": [
        {
          "hostPath": "${APP_DATA_DIR}/data/config",
          "containerPath": "/config"
        },
        {
          "hostPath": "${APP_DATA_DIR}/data/downloads",
          "containerPath": "/downloads"
        }
      ],
      "dependsOn": ["gluetun"]
    }
  ]
}
